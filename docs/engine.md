# グライド入力エンジン

[READMEに戻る](../README.md) · [全体設計](architecture.md) · [開発手順](development.md)

`GlideDecoder` は、ストロークから得た幾何モデル A と、文脈・読みの途中までから得た文字言語モデル B を、文字を追加する段階で結合します。この探索中に、同梱 Sumire 辞書の読み・単語コスト・品詞接続コストも評価します。その後、候補の読みを辞書ベースの `KanaKanjiConverter` に渡します。文字言語モデルに漢字変換をさせる構成ではありません。

## A: ストロークの確率格子

`StrokeModel.lattice(trace)` は Android に依存しない処理です。座標をキーの幅・高さで正規化し、疎なタッチ座標の間を補間して、通過したキーの区間を求めます。各区間は「かなを 1 文字出力」または「通過して出力しない」の分布 `StrokeEvent` になります。

- 最初と最後の区間は文字を出力します。
- 途中の直線的な通過は省略しやすく、曲がり角やキー中心での滞留は文字を出力しやすくします。
- 文字の分布はキー中心からの距離で決まり、周辺キーも候補になります。既定では各区間の代表点から距離1.5以内の最大9キーを対象にし、中央付近では上下左右・斜めの隣接キーも探索できます。距離は横をキー幅、縦をキー高さで割った座標で測ります。表の端や空きマス付近でも、枠を埋めるために遠いキーを採用しません。
- 同じキーから離れて戻る操作や、キー内で中心から離れて戻る小さなループは連続する同じ文字を表せます。静止した長押しはイベント数を増やしません。デコーダーでは、戻る動きがない場合も下記の連続文字補完を候補に含めます。
- 濁音・半濁音・小書き文字は元のキーから固定の事前確率で出します。例えば `か` は `か/が/ゕ`、`や` は `や/ゃ` を表せます。

各イベントについて、文字の確率とスキップの確率の和は 1 です。格子の各経路の確率はイベント選択の確率の積です。同じ読みになる経路の確率を合算すると `A(reading | stroke)` になります。意味、単語辞書、直前の文章はこのモデルに入りません。

現在の距離・曲率・滞留・異体かなの重みはヒューリスティックです。実際の指の軌跡から学習・校正した認識率を主張するものではありません。直線の途中にある文字を意図的に入力したか、単に通過したかは幾何情報だけでは一意に定まりません。

`StrokeModelConfig` の既定値は `nearbyKeyCount = 9`、`maxNearbyKeyDistance = 1.5`、`distanceSigma = 0.55` です。移動中の距離の重みは `exp(-distance² / (2 * distanceSigma²))`。タップなど1区間だけの軌跡と、キー中心で `centerDwellMillis`（既定140ms）以上滞留した区間では、`preciseDistanceSigma = 0.43` と `distanceSigma` の小さい方を使います。明確に選んだキーの位置精度を保ちながら、移動中のずれを許容します。従来の近傍3キー・sigma 0.43では、候補から除外された文字は辞書や言語モデルでも復元できず、隣接キーにも強い減点がかかっていました。探索範囲とsigmaを広げ、自然な読みが多少の座標ずれを上回れるようにしています。新しいイベントや任意の文字の挿入は追加せず、既存の通過点のスキップと連続文字補完を使います。従来との比較には `nearbyKeyCount = 3, distanceSigma = 0.43, maxNearbyKeyDistance = 16.0` を指定します（最後の値は通常の五十音表全体を含む距離）。

### 連続文字の省略

`こ→ま→よ` の軌跡から `ごままよ` のような読みを探せるよう、1イベントで選んだ同じかなを既定で最大3文字まで出力できます。先頭・途中・末尾の繰り返しに対応し、濁音・半濁音・小書き文字も選んだ形のまま繰り返します。例えば `ばば` は生成しますが、1イベントから `はば` へは変えません。

デコーダーはイベントの出力確率を、1文字 `0.85`、2文字 `0.1275`、3文字 `0.0225` に分配します。距離と濁音などの事前確率は一度だけ計上し、B は追加した文字ごとに新しい prefix で評価します。辞書も繰り返し後の読みで評価します。スキップしたイベントからは文字を増やしません。読み長上限に達した場合はそこで停止し、同じ読みの異なる経路は従来どおり確率を合算します。

設定は `maxRepeatedCharactersPerEvent = 3` と `repetitionProbability = 0.15` です。前者を `1`、または後者を `0.0` にすると補完を無効にできます。既定では `repeatedBeamSlots = 4` 本を探索中の繰り返し候補に確保し、返却時は最大2本を確保します。短い読みのスコアが高い場合でも、繰り返し候補を選択できるようにするためです。枠が小さい場合は最上位候補と既存の未知語枠を優先し、表示順はスコア順に保ちます。

`ReadingCandidate.hasImplicitRepetition` は、省略した連続文字を補う経路を含むことを示します。候補エンジンはその読みをかなのまま表示する枠も確保します。漢字への変換候補で埋まり、目的のかな表記を選べなくなることを防ぎます。候補数・ビーム幅・読み長の上限は維持します。

## B: かな 1 文字ずつの予測

`KanaLanguageModel.nextLogProbabilities(context, prefixes)` は、同じ文脈に対する複数の読みの途中までをまとめて受け取り、各 prefix に対応する「次のかな」の自然対数確率を返します。デコーダーは同じ呼び出し内で prefix ごとに結果をキャッシュします。異なる文脈・異なる入力をまたいだキャッシュは持ちません。

`hukuda222/hiragana-gpt2-xsmall` の ONNX 実装はこのインターフェースに接続されます。モデルファイルの取得・配置方法は [ビルド準備](development.md#ビルド準備)、仕様とライセンスは [ひらがな言語モデル](hiragana-model.md) を参照してください。確率が存在しない文字は B の下で出現不可能として扱います。モデルが利用できない場合の扱いは上位の候補エンジンが担当し、デコーダー自身は架空のモデル出力を作りません。

## 結合とビーム探索

各イベントで、現在の候補について B をバッチ評価し、スキップと文字追加を展開します。同じ読みを持つ候補は A の対数確率を log-sum-exp で合算してから、次のスコアで枝刈りします。

```text
score = strokeWeight * log A
      + languageWeight * log B
      + characterInsertionBonus * 読みの文字数
      - dictionaryWeight * dictionaryCost
```

既定値は `strokeWeight = 1.0`、`languageWeight = 0.35`、`characterInsertionBonus = 1.5`、`dictionaryWeight = 1.0` です。B は次の文字の条件付き確率の積で、EOS の確率は加えていません。この積には短い読みを好む傾向があるため、読みの1文字につき `1.5` を加算します。最終候補だけでなく、探索中の枝刈りにも適用し、曲がり角や滞留で選んだ文字が途中で脱落しにくくします。

既定の言語モデル重みでは、かなをほぼ一様に予測する場合の1文字あたり約 `-1.56` をおおむね相殺する値です。一方、直線通過の出力対スキップのコスト差 `ln(0.9 / 0.1) ≈ 2.20` や、既定の連続文字補完の追加コスト（約 `1.73`〜`1.90`）より小さく、補正だけで通過キーや連続文字を優先させないようにしています。これは各経路の局所的な比較であり、辞書・文脈・経路の合算による順位を保証するものではありません。

`characterInsertionBonus = 0.0` で補正前の評価に戻せます。`languageWeight` と文字追加ボーナスは、実際のストロークと正解読みの評価用データを用いて調整してください。

`ReadingLexicon.newSession()` が探索1回の辞書状態を作り、読みを1文字延ばすたびに LOUDS の読みトライと語分割の Viterbi 状態を更新します。単語の途中として存在するprefixも候補に残ります。最終イベントでは、未完prefixの仮スコアではなく完了した語分割・EOSのコストを使ってから枝刈りします。同じprefixの累積辞書コストをその都度置き換えるため、辞書コストをイベントごとに重ねて加算することはありません。

`dictionaryCost` は生の単語・品詞接続コストに語境界ペナルティを加えてスケールした評価値であり、対数確率ではありません。語境界のコストで短語の過剰な連結を抑え、辞書にない文字にも高コストの有限経路を用意しています。既定では16本のビーム中2本をA+B側の順位で残し、返却候補にも1本の枠を確保します（幅・返却数が1の場合は辞書混合の最上位を優先）。これにより人名・新語を完全に排除せず、表示順は混合スコア順に保ちます。

辞書を使わない比較は `dictionaryWeight = 0.0`。辞書のセッション作成・評価を省略し、以前のA+B探索になります。辞書を渡さない `GlideDecoder` もA+Bのみです。具体的なコストと探索上限は [変換エンジン](conversion.md) を参照してください。Aの確率格子には辞書情報を入れていません。

`ReadingCandidate` は読み、A の対数確率、B の対数確率、辞書コスト、最良の語分割の未知文字数、結合スコア、`posterior` を返します。A は枝刈り後に残った経路についての近似です。`posterior` は**返却候補だけ**に対するスコアの softmax であり、すべての読みについての厳密な事後確率や、校正済みの認識信頼度ではありません。返却件数を変えると `posterior` の値も変わります。

`languageWeight = 0.0` では言語モデルを呼びません。この場合 `languageLogProbability` は未評価を表す `0.0` です。文字追加ボーナスは言語モデルと独立した項なので、Aのみを確認するには `dictionaryWeight = 0.0` と `characterInsertionBonus = 0.0` も指定します。

## 計算量と中断

最終表示では、各読みの漢字候補を `GlideCandidateEngine` が共通スコアで比較する。
読みの混合スコアから、その読みの最良変換とのコスト差を引くため、有力な読みの
第2候補が弱い別読みの第1候補より上に出る。かな表記と別読みの枠も保持する。
計算式・重み・重複排除は [最終候補の順位付け](conversion.md#最終候補の順位付け) を参照。

既定では入力点 8,192 個、補間点 1,024 個、格子 48 イベント、ビーム幅 16、読み長 32 文字、返却 8 候補までです。1 イベントにつき最大 `maxRepeatedCharactersPerEvent` 回（既定3回）のモデル呼び出しを行い、各バッチをビーム幅以内に抑え、既に評価した prefix は再計算しません。イベント数を超える落書きのような入力や、不正な座標・逆転した時刻は候補なしとして扱います。

デコーダーはスレッドの割り込みを保持したまま `InterruptedException` を送出します。既定 5 秒の時間制限を処理の区切りで確認し、超過時は `GlideDecodeTimeoutException` を送出します。実行中のネイティブ推論の中断は `KanaLanguageModel.cancelPendingInference()` とモデル実装が担当します。時間制限は割り込み不能な外部実装の処理時間までは保証できません。

## 検証

### 周辺キーの探索範囲と距離減点の調整（2026-09-15）

近傍3キー・sigma 0.43から、距離1.5以内の最大9キー・移動中のsigma 0.55へ変更した。
1区間だけの軌跡とキー中心に140ms以上滞留した区間はsigma 0.43を維持する。
ビーム幅・言語モデル重み・辞書重み・文字追加ボーナスは共通とし、
Android 17エミュレーターの実モデル・実辞書で同一の合成軌跡を比較した。
各キー中心を300ms間隔で結び、静止サンプルを入れず、意図した文字の1つを隣のキーへずらした。

| 期待する読み | なぞるキー | 変更前 | 変更後 |
| --- | --- | --- | --- |
| ねこ | ぬ→こ | 上位8件外 | 3位 |
| こんにちは | こ→ん→に→つ→は | 1位 | 1位 |
| にほんご | に→ほ→ん→け | 2位 | 1位 |
| ありがとう | あ→り→か→と→く | 1位 | 1位 |

「にほんご」の前文脈は「わたしは」、他は空。濁音は元のキーを使用する。
表示候補にも「ネコ」と「日本語」が残ることを確認した。
この4例の変更後の読み探索は、モデル読込後で216〜1,061ms（各1回、漢字変換時間を除く）。
既存の滞留なし合成軌跡でも「こんにちは」「ありがとう」「にほんご」が1位に残り、
「胃が痛い」の上位3件、滞留ありの `こ→ま→よ` から「ごままよ」の候補保持を確認した。
`NearbyKanaTest` は全方向の隣接キーの探索・遠いキーの除外・確率の正規化、
旧格子から落ちる文字の辞書による救済、移動中の文脈補正とタップ・滞留の位置精度、
言語モデルが一様なときに通過キーや連続文字を最上位へ追加しないことを検証する。
単体テスト102件、実モデルを使う端末テスト6件、Lint、debug APKと端末テストAPKのビルドが成功した。

少数の合成軌跡での結果であり、実際のユーザー入力での精度は未評価。
軌跡を外れた自然な語を選べる範囲を広げる変更で、不自然な語をすべて除外するものではない。
隣接する別の語への誤認識や、下位の不要な連続文字候補は残る。

### 短い読みへの偏りの補正（2026-09-15）

`characterInsertionBonus` の既定値を `0.0` から `1.5` に変更した。
現行のわ列配置、同じ滞留なしの合成軌跡・文脈・実モデル・実辞書を使用し、
Android 17エミュレーターで補正前後の読み順位を比較した。

| 読み | 補正前 | 補正後 |
| --- | --- | --- |
| こんにちは | 2位（先頭は「こんな」） | 1位 |
| ありがとう | 1位 | 1位 |
| にほんご | 2位（先頭は「にこ」） | 1位 |

「日本語」が表示候補の上位3件に入り、既存の「胃が痛い」の上位3件と
`こ→ま→よ` からの「ごままよ」の候補保持も確認した。
単体テスト98件、実モデルを使う端末テスト5件、Lint、debug APKと端末テストAPKのビルドが成功した。
追加した単体テストでは、曲がり角・滞留の文字が残ること、幅1の探索でも文字が脱落しないこと、
直線の通過キーを省けること、確率1で同じ文字を予測してもタップの最上位が1文字になることを確認した。

実際のユーザー軌跡での精度は未評価。この補正は文字省略を禁止するものではなく、
辞書や文脈による別の読みへの誤認識、不要な連続文字の候補は引き続き残る。
以下の過去の比較テストは補正を無効にして保持し、今回の結果と区別する。

### 3ブランチ統合時の確認（2026-09-15、長さ補正の導入前）

この節の結果は `characterInsertionBonus = 0.0` での記録。以下はUI更新を取り込む前の統合結果。最新mainのわ列配置（わ・を・ん・空き・ー）では、
同じ生成方法の「にほんご」の合成軌跡が変わり、読み順位は辞書なし3位→あり2位になった。
短い「にこ」が先頭に残る。旧配置での1位という結果と区別し、新配置の実機回帰テストに記録する。

変換順位の文脈評価、連続かな補完、未知かなを含む読みの部分変換を統合した。
候補は共通スコアで順位付けし、元の最上位読みと連続文字を補ったかな表記の枠を確保する。
近い読みの代表を先頭に並べる処理も維持する。未知かなを含む読みもスコア付き変換へ渡す。

JVMテスト88件、接続端末でのAndroidテスト5件、debug APKビルド、lintが成功した。
実機では実モデル・実辞書と合成軌跡で「胃が痛い」が上位3件に残ること、
`こ→ま→よ` から「ごままよ」が表示候補に残ること、既存の挨拶・日本語変換を確認した。
実際のユーザー軌跡での精度を保証する結果ではない。

`RepeatedKanaTest` は、戻り動作のない先頭・途中・末尾の連続文字、濁音、3連続、上限・無効化、確率の正規化・経路合算、辞書の完了評価、候補保持枠、かな表記の表示を検証します。制御した言語モデルによる探索範囲のテストに加え、`GlideEngineInstrumentedTest.realPipelineOffersGomamayoWithoutReturningToMa` は実モデル・実辞書で `こまよ` から `ごままよ` が表示候補に含まれることを検証します。

連続文字補完ありの実端末 A142 / Android 16 では、各キー中心に180ms滞留する合成軌跡 `こ→ま→よ` から `ごままよ` が表示され、モデル読込後の候補生成は約0.9秒でした。キーへの戻りやループは不要です。一方、同じ3キーの中心を滞留なしで結んだ軌跡では `ま` を省略する読みが優先され、`ごままよ` は表示候補に残りませんでした。連続文字の探索経路を追加する変更であり、すべての軌跡での認識を保証するものではありません。

既存の滞留なし合成軌跡では、辞書ありの `ありがとう` と `にほんご` は引き続き1位、`こんにちは` は2位でした。辞書なしでは、繰り返し回数への確率分配で短い読みが有利になり、`ありがとう` は2位、`にほんご` は3位になりました。

`GlideDecoderTest` は直線の途中のキーの省略、角と滞留の認識、戻り・小ループと長押しの区別、濁音と小書き文字の文脈選択、A の文脈非依存性、経路確率の合算、確率の正規化、prefix のバッチ化とキャッシュ、長さ補正、無効入力、中断、プロバイダーの不正応答を検証します。

`DictionaryBeamTest` は辞書を使わないと途中で落ちる読みの救済、最終イベントでの完了コストの評価、累積コストの二重加算防止、未知語経路の保持、辞書無効時の挙動を検証します。辞書で未知文字を含むと判断した読みも変換器へ渡し、未知のかなを残しながら既知の部分を変換します。最上位の元の読みも、候補数に余裕がある場合はUI候補に残します。ただし、短い辞書語へ分解できる不自然な列は未知扱いにならないため、辞書だけで非単語をすべて除去できるわけではありません。

`GlideCandidateEngineTest` は部分変換、未知語だけの読み、変換候補なしの場合、重複排除と候補数上限を検証します。`SumireKanaKanjiConverterTest` は実辞書で未知かなが文頭・文中・文末にある場合の部分変換を検証します。

連続文字補完の導入前の Pixel 8 の実モデル比較では、キー内のずれと丸い曲がりを含み、各キーに滞留しない同一の合成軌跡について次の結果を確認しました。LLM重み・ビーム幅・Aの設定は共通で、変更は辞書混合の有無だけです。

| 正解の読み | 辞書なしの正解順位 | 辞書ありの正解順位 |
| --- | --- | --- |
| こんにちは | 4 | 2 |
| ありがとう | 1 | 1 |
| にほんご | 2 | 1 |
| とうきょう | 上位8件外 | 上位8件外 |

この4件の候補生成は、モデル読込後で辞書なし138〜335ms、あり276〜559msでした。1回の合成軌跡比較であり、実利用の認識率・応答時間の保証ではありません。軌跡による文字抜け、候補外の正解、辞書に存在する別単語への誤認識は引き続き改善対象です。

```powershell
.\gradlew.bat :app:testDebugUnitTest
```
